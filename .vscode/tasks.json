{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build lex/yacc stuff",
            "type": "shell",
            "command": "${workspaceFolder}/lex_yacc/build_yacc.sh",
            "options": {
                "cwd": "${workspaceFolder}/lex_yacc"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}